## 1. Flet Environment Setup

- [x] 1.1 Install Flet and necessary dependencies (flet, firebase-admin, etc.)
- [x] 1.2 Initialize Flet application structure in `main.py`
- [x] 1.3 Configure Firebase Admin SDK with service account key

## 2. SQLite Database Schema

- [ ] 2.1 Create `database.py` to handle SQLite connections
- [ ] 2.2 Define schema for `tasks` table (id, title, status, timestamp)
- [ ] 2.3 Define schema for `notes` table (id, content, timestamp)
- [ ] 2.4 Define schema for `shopping_list` table (id, item, checked, timestamp)

## 3. UI Implementation (Mockup)

- [ ] 3.1 Implement Vertical Editor layout with `ft.TextField` (Markdown)
- [ ] 3.2 Implement Real-time Preview Pane with `ft.Markdown`
- [ ] 3.3 Implement Shopping List View with `ft.Checkbox` and `ft.ListView`
- [ ] 3.4 Integrate Google Calendar View component
- [ ] 3.5 Set up basic navigation (Tabs or Drawer) between views
