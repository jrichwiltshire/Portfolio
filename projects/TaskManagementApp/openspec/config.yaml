schema: spec-driven

context: |
  Tech stack: Python, Flet (UI), Firebase Firestore (real-time sync), Railway (hosting)
  Target platforms: desktop browser (PWA) and Android (Pixel 8, installed via Chrome)
  Developer preference: the user writes all implementation files themselves.
  When implementing, provide code snippets and explanations in chat rather than
  writing files directly, unless explicitly asked to do so.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
